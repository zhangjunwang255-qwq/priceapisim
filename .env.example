# Railway 环境变量（本地测试用）
# 复制此文件为 .env 并填入实际值

# Railway PostgreSQL（部署后自动注入，无需手动设置）
# DATABASE_URL=postgresql://...

# 天勤模拟账户（必填）
TQ_ACCOUNT=你的快期账号
TQ_PASSWORD=你的快期密码

# 订阅的合约代码（逗号分隔）
SYMBOLS=GFEX.pb2406,GFEX.pd2406

# 端口（Railway 自动设置，无需手动）
# PORT=8080
